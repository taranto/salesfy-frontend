import { StyleSheet } from "react-native";

const styles: any = StyleSheet.create({
	view: {
		display: "flex",
		flex: 5,
		flexDirection: "row",
		marginBottom: 10
	},
	viewFooter: {
	}
});
export default styles;
